
NAME_55555 = [];


for(i = 0; i < 5; i++) {
  value = NAME_55555[ i ];

  /* You'd think this would work, but what is happening
    is typeoif takes the type of NAME_55555 and then the subscript operator
    gets the value at index i of the value returned by typeof

    typeof NAME_55555 == "array";
    "array"[i]
    @TODO refactor typeof grammar to allow for a primary expression on the rightt
   */
  print( typeof NAME_55555 [ i ] );
}
